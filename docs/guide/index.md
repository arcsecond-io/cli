# Install Arcsecond CLI

Simply issue the folliwing in a Terminal:

```bash
$ pip install arcsecond
```

To upgrade an existing Arcsecond installation:

```bash
$ pip install --upgrade arcsecond
```

Use the CLI now:
* In the terminal